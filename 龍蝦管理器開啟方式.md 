啟動測試 
bun run tauri dev
cd "/home/reamaster/openclaw-manager/openclaw-manager" && timeout 30 bun run tauri dev 2>&1 | tail -40 || true


# 直接運行編譯好的二進制
cd "/home/reamaster/openclaw-manager/openclaw-manager/src-tauri/target/debug"
./openclaw-manager
[2026-03-15T12:33:41Z INFO  openclaw_manager] 🦞 OpenClaw Manager 启动
VM 0x7ead61414000 on pid 47356 received NeedDebuggerBreak trap


cd "/home/reamaster/openclaw-manager/openclaw-manager" && bun run tauri dev 2>&1 &
sleep 15


需要 sudo 權限。請手動運行：
sudo curl -fsSL https://tailscale.com/install.sh | sh
或者分步驟運行：
# 1. 添加 Tailscale 倉庫
curl -fsSL https://tailscale.com/install.sh | sh
# 2. 啟動 Tailscale
sudo tailscale up
安裝完成後，刷新管理器頁面，VPN 防護就會顯示為「啟用」狀態。

Tailscale 似乎沒有完全安裝。請運行以下命令完成安裝：
# 方法 1：使用官方安裝腳本
curl -fsSL https://tailscale.com/install.sh | sh
# 方法 2：手動安裝
sudo apt update
sudo apt install tailscale
# 然後啟動
sudo tailscale up
安裝並啟動 Tailscale 後，刷新管理器頁面即可看到 VPN 防護狀態更新為「啟用」。


